node_modules/
.expo/
.DS_Store
*.zip
dist/
web-build/

# Local-only artifacts (builds, keys, logs) — see noor-extra/
noor-extra/
crash.log

# Signing keys — NEVER commit
*.jks
*.keystore
upload_certificate.pem
credentials.json
credentials/

# Build artifacts
build-*.aab
build-*.apk
*.aab
*.apk
*.apks
noor-signed.apks
noor_apk/
